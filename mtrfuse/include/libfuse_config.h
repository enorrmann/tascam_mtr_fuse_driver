/*
 * libfuse_config.h — archivo de configuración generado.
 *
 * En un build real de libfuse, meson genera este archivo con los
 * resultados de las comprobaciones del sistema y con la macro
 * LIBFUSE_BUILT_WITH_VERSIONED_SYMBOLS (símbolos versionados por ABI).
 *
 * Para depurar/compilar un driver enlazando contra la libfuse3 presente
 * en el sistema (instalada como libfuse3.so.x con símbolos versionados),
 * definimos SOLO la macro que afecta a la API pública. El resto de
 * comprobaciones de bajo nivel pertenecen a lib/, no a los headers que
 * este driver incluye (fuse.h / fuse_common.h / fuse_opt.h / fuse_log.h).
 */
#ifndef LIBFUSE_CONFIG_H_
#define LIBFUSE_CONFIG_H_

#define LIBFUSE_BUILT_WITH_VERSIONED_SYMBOLS 1

#endif /* LIBFUSE_CONFIG_H_ */
